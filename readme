baixar

npm install para baixar dependencias

por enquanto somente lodash e electron (para interface).

para rodar na linha de comando:
node index.js

para rodar a interface em electron ( em construção):
instalar o electron:
    npm install --global electron
rodar na pasta do projeto: npm start