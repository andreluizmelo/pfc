baixar

npm install para baixar dependencias:
    lodash, bootstrap, jquery, etc.

para rodar na linha de comando:
node index.js

para rodar a interface em electron ( em construção ainda):
instalar o electron na primeira vez:
    npm install --global electron
rodar na pasta do projeto: npm start